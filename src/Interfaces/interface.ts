export interface UserInterface{
    userName: string;
    email: string;
    password: string;
    profilePhoto?: string;
}

export interface StatusMessage{
    status: number; 
    message: string 

}